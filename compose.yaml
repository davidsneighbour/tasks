services:
  app:
    build: .
    ports:
      - "3070:3070"
    env_file:
      - .env
    environment:
      DATABASE_URL: /app/data/tasks.sqlite
      HOST: 0.0.0.0
      PORT: 3070
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3070/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3
